# Backend (Node.js)

Запуск:

1. Скопируйте `.env.example` в `.env` и настройте `DATABASE_URL`, `JWT_SECRET`, `PYTHON_SERVICE_URL`.
2. Установите зависимости:

```bash
cd backend
npm install
```

3. Запустите сервер:

```bash
npm run dev
```

Эндпоинты реализованы в `src/`.
